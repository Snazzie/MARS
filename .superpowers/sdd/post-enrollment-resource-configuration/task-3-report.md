
Review fixes: installer commands now launch scripts without argv code; POSIX installers securely prompt on controlling TTY and PowerShell uses `Read-Host -AsSecureString`. Configuration mutation exposes accepted refresh and error state for retry integration.
