name: macOS runner smoke
on:
  workflow_dispatch:
jobs:
  smoke:
    runs-on: [self-hosted, macOS, ARM64, whitesmith-macos]
    steps:
      - name: Inspect runner
        run: |
          sw_vers
          uname -a
          sysctl -n hw.ncpu
