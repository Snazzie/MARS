import type { ActionGraph as ActionGraphDto } from "@mars/contracts";

export function ActionGraph({ graph }: { graph: ActionGraphDto }) {
  const columns = Math.max(1, Math.ceil(Math.sqrt(graph.nodes.length)));
  const positions = new Map(graph.nodes.map((node, index) => [node.id, { x: (index % columns) * 190 + 90, y: Math.floor(index / columns) * 90 + 38 }]));
  return <section className="graph-panel" aria-labelledby="graph-title"><div className="panel-kicker" id="graph-title">Action dependency graph</div><div className="action-graph-wrap"><svg className="action-graph-svg" viewBox={`0 0 ${columns * 190} ${Math.max(90, Math.ceil(graph.nodes.length / columns) * 90)}`} role="img" aria-label="Action dependency relationships">{graph.edges.map((edge) => { const from = positions.get(edge.from); const to = positions.get(edge.to); return from && to ? <line className="graph-edge" key={`${edge.from}-${edge.to}`} x1={from.x} y1={from.y} x2={to.x} y2={to.y} /> : null; })}{graph.nodes.map((node) => { const position = positions.get(node.id)!; return <g className="graph-svg-node" key={node.id} tabIndex={0} transform={`translate(${position.x - 72},${position.y - 22})`}><rect width="144" height="44" rx="6" /><text x="10" y="18">{node.name.slice(0, 20)}</text><text className="graph-svg-status" x="10" y="34">{node.status.replaceAll("_", " ")}</text></g>; })}</svg></div><details className="graph-fallback"><summary>View dependency table</summary><table><caption className="sr-only">Action dependencies</caption><thead><tr><th>Action</th><th>Depends on</th></tr></thead><tbody>{graph.nodes.map((node) => <tr key={node.id}><th>{node.name}</th><td>{graph.edges.filter((edge) => edge.to === node.id).map((edge) => graph.nodes.find((candidate) => candidate.id === edge.from)?.name ?? edge.from).join(", ") || "—"}</td></tr>)}</tbody></table></details></section>;
}
